import React from "react";
import { IRePost } from "../../../components/post/types";

interface Props {
  post: IRePost;
}
const PostDetailsRePost: React.FC<Props> = ({ post }) => {
  return <div>PostDetailsRePost</div>;
};

export default PostDetailsRePost;
