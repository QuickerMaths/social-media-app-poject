describe("Posts route integration tests", () => {
  it("should return all posts", () => {
    expect(1).toBe(1);
  });

  it("should return a post by id", () => {
    expect(1).toBe(1);
  });

  it("should return all posts by user id", () => {
    expect(1).toBe(1);
  });
});
